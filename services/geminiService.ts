// AI features have been removed from this application.
// This file is kept as a placeholder to prevent import errors if any references remain,
// but all functional logic has been stripped.

import { Grammar, CanonicalState } from "../types";

export const explainState = async (state: CanonicalState, grammar: Grammar): Promise<string> => {
  return "";
};

export const analyzeGrammar = async (grammarText: string): Promise<string> => {
    return "";
};